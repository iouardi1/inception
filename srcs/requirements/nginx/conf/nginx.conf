# events {
#   worker_connections  1024;
# }

# http {
# 	# Upstream to abstract backend connection(s) for php
# 	upstream php {
# 		server unix:/tmp/php-cgi.socket;
# 		server 127.0.0.1:9000;
# 	}

# 	server {
# 		## Your website name goes here.
# 		server_name ihsan.cc;
# 		listen		443;
# 		## Your only path reference.
# 		root /var/www/wordpress;
# 		## This should be in your http block and if it is, it's not needed here.
# 		index index.php;

# 		ssl    on;
# 		ssl_certificate    /etc/ssl/ihsanCA.crt;
# 		ssl_certificate_key    /etc/ssl/ihsanCA.key;
# 		# location = /favicon.ico {
# 		# 	log_not_found off;
# 		# 	access_log off;
# 		# }

# 		# location = /robots.txt {
# 		# 	allow all;
# 		# 	log_not_found off;
# 		# 	access_log off;
# 		# }

# 		location / {
# 			# This is cool because no php is touched for static content.
# 			# include the "?$args" part so non-default permalinks doesn't break when using query string
# 			try_files $uri $uri/ /index.php?$args;
# 		}

# 		location ~ \.php$ {
# 			#NOTE: You should have "cgi.fix_pathinfo = 0;" in php.ini
# 			include fastcgi_params;
# 			fastcgi_intercept_errors on;
# 			fastcgi_pass php;
# 			#The following parameter can be also included in fastcgi_params file
# 			fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
# 		}

# 		# location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
# 		# 	expires max;
# 		# 	log_not_found off;
# 		# }
# 	}
# }


server {
	listen 443 ssl;
	listen [::]:443 ssl;

	server_name iouardi.42.fr;
	# On note le cert et la key (necessaire protocole ssl)
	ssl_certificate		/etc/nginx/ssl/iouardi.crt;
	ssl_certificate_key	/etc/nginx/ssl/iouardi.key;

	# Protocole d'encryptions pour les cles ssl
	ssl_protocols		TLSv1.2 TLSv1.3;

	# Fichiers a afficher et dossier ou les chercher
	root /var/www/html;
	index index.php index.nginx-debian.html;

	# Directive necessaire pour les endpoints, 
	# Fast cgi necessaire pour nginx pour "traduire" le php
	location / {
		try_files $uri $uri/ /index.php$is_args$args;
	}

	location ~ \.php$ {
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
		fastcgi_pass wordpress:9000; #PHP for wordpress will listen on the port 9000
		fastcgi_index index.php;
		include fastcgi_params;
		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
		fastcgi_param SCRIPT_NAME $fastcgi_script_name;
	}
}